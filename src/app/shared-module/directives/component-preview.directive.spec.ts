import { ComponentPreviewDirective } from './component-preview.directive';

describe('ComponentPreviewDirective', () => {
  it('should create an instance', () => {
    const directive = new ComponentPreviewDirective();
    expect(directive).toBeTruthy();
  });
});
