import { ComponentPrototypeDirective } from './component-prototype.directive';

describe('ComponentPrototypeDirective', () => {
  it('should create an instance', () => {
    const directive = new ComponentPrototypeDirective();
    expect(directive).toBeTruthy();
  });
});
