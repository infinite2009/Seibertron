import { TreeDirective } from './tree.directive';

describe('TreeDirective', () => {
  it('should create an instance', () => {
    const directive = new TreeDirective();
    expect(directive).toBeTruthy();
  });
});
