// 表单控件类型
enum ControlType {
  Code = 'code',
  number= 'number',
  text = 'text',
  checkbox = 'checkbox',
  select = 'select',
  cascade = 'cascade',
  radio = 'radio',
  tree = 'tree',
}

export default ControlType;
