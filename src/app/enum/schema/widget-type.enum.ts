enum WidgetType {
  text = 'text',
  link = 'link',
  image = 'image',
  input = 'input',
  radio = 'radio',
  checkbox = 'checkbox',
  form = 'form',
  list = 'list',
  tree = 'tree',
  matrix = 'matrix',
}

export default WidgetType;
