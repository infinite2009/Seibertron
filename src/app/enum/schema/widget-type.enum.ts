enum InsertType {
  container = 'container',
  text = 'text',
  link = 'link',
  image = 'image',
  input = 'input',
  radio = 'radio',
  checkbox = 'checkbox',
  form = 'form',
  list = 'list',
  table = 'table',
  tree = 'tree',
  component = 'component',
  state = 'state',
  event = 'event',
  dataSource = 'dataSource',
}

export default InsertType;
