enum WidgetType {
  container = 'container',
  text = 'text',
  link = 'link',
  image = 'image',
  input = 'input',
  radio = 'radio',
  checkbox = 'checkbox',
  form = 'form',
  list = 'list',
  table = 'table',
  tree = 'tree',
  component = 'component',
}

export default WidgetType;
